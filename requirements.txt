# Gmail Account Creation Automation — Dependencies
#
# Python 3.9+ required (PEP 604 union types NOT used; compatible with 3.9)
#
# Core:
#   playwright         → browser automation (Chromium)
#   twocaptcha         → 2Captcha reCAPTCHA solver (already installed: 2captcha-python)
#   requests           → HTTP calls (5sim API, QR decode API)
#   pyzbar             → QR code decoding (requires libzbar system lib; optional)
#   Pillow             → image handling for QR screenshots
#
# Install:
#   pip3 install playwright 2captcha-python requests pyzbar Pillow
#   python3 -m playwright install chromium

playwright>=1.40.0
2captcha-python>=0.0.3
requests>=2.28.0
Pillow>=10.0.0
# pyzbar>=0.1.9  # optional — only needed if using local zbar for QR decode
# pyzbar needs the zbar system library (brew install zbar / apt install libzbar0)
# If zbar is not available, the framework falls back to an online QR decode API.
